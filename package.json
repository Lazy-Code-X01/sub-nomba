{
  "name": "sub",
  "version": "1.0.0",
  "private": true,
  "description": "Sub — subscription billing engine monorepo",
  "workspaces": [
    "apps/server",
    "apps/client"
  ],
  "scripts": {
    "server": "npm run dev --workspace=apps/server",
    "client": "npm run dev --workspace=apps/client",
    "test": "npm run test --workspace=apps/server",
    "build": "npm run build --workspace=apps/server && npm run build --workspace=apps/client"
  }
}
